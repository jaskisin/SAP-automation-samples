name: Access YAML Files in SAP Directory

on:
  workflow_dispatch:

jobs:
  access-yaml-files:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: List YAML files
      run: |
        echo "Listing YAML files in the SAP directory:"
        find SAP -name '*.yaml'
      
    - name: Process YAML files
      run: |
        for file in $(find SAP -name '*.yaml'); do
          echo "Processing $file"
          # Add your processing commands here
        done
